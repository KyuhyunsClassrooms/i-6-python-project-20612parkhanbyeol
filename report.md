# 최종 보고서

## 1. 프로그램 주제와 목적
  프로그램의 주제:

> 사용자가 원하는 부위, 난이도, 시간에 딱 맞춰 운동을 골라주는 똑똑한 운동 루틴 추천기
 이 프로그램을 만든 이유:

> 바쁜 일상 속에서 운동을 계획할 때 "오늘 무슨 운동을 조합해야 하지?"라는 선택의 피로감을 줄이기 위함입니다. 사용자가 원하는 운동 부위와 가용 시간을 입력하면, 조건에 맞는 최적의 운동 루틴을 자동으로 구성해 줌으로써 시간 낭비를 막고 효율적인 운동 체계를 제공하고자 만들었습니다.

---

## 2. 문제 분해 및 모델링

### 입력

* 사하고 싶은 운동 부위, 내 실력에 맞는 난이도, 내가 낼 수 있는 최대 시간
* 이름, 소요 시간, 소모 칼로리, 부위, 난이도)

### 처리

1. 사용자가 부위나 난이도에 오타를 내면 올바르게 쓸 때까지 계속 다시 물어보고, 시간에 글자를 치면 에러가 나지 않게 숫자로 쓰라고 안내합니다.
2. 메뉴판 데이터 중에서 사용자가 고른 부위와 난이도가 동시에 딱 맞는 운동들만 먼저 쏙 골라냅니다.
3. 매번 똑같은 순서로 운동하면 지루하니까 운동 카드를 마구 섞어줍니다.
4. 운동들을 하나씩 꺼내서 시간을 더해보며, 내가 정한 제한 시간을 넘지 않을 때까지만 장바구니에 차곡차곡 담습니다.

### 출력

* '오늘의 맞춤 운동 루틴 목록'
* '실제 총 운동 시간' 및 '예상 총 소모 칼로리'

---

## 3. 사용한 2차원 리스트 설명

2차원 리스트 이름: `exercise_db`

한 행의 의미:

> 하나의 독립된 운동 종목이 가지는 속성 정보의 집합

각 열의 의미:

| 열 번호 | 의미 | 실제 예시 |
| --- | --- | --- |
| 0번 열 | 운동 이름 (문자열) | "스쿼트", "푸쉬업" |
| 1번 열 | 운동 소요 시간 (정수, 분 단위) | 10, 5 |
| 2번 열 | 예상 소모 칼로리 (정수, kcal 단위) | 120, 50 |
| 3번 열 | 운동 타겟 부위 (문자열) | "하체", "상체" |
| 4번 열 | 운동 난이도 (문자열) | "초급", "중급", "상급" |

이 2차원 리스트를 사용한 이유:

> 하나의 운동을 표현하기 위해서는 이름, 시간, 칼로리, 부위, 난이도라는 서로 다른 형태의 데이터들이 유기적으로 연결되어 있어야 합니다. 2차원 리스트를 사용하면 인덱스 번호(0~4)를 활용해 데이터의 연관성을 유지하면서 대량의 운동 목록을 행 단위로 깔끔하고 체계적으로 관리할 수 있기 때문입니다.

---

## 4. 함수 설명

| 함수 이름 | 역할 | 입력값 | 반환값 또는 출력 |
| --- | --- | --- | --- |
| `get_user_input` | 사용자가 오타를 내거나 잘못 입력하면 정상 값을 쓸 때까지 감시하며 입력받음 | 없음 | `part`, `level`, `time` |
| `generate_easy_routine` | 조건에 맞는 운동을 필터링하고 제한 시간 내로 루틴을 조합함 | `max_time`, `preferred_part`, `preferred_level` | `selected_routine` (선택된 운동들의 2차원 리스트) |
| `print_result` | 최종 추천 루틴과 총 시간, 총 칼로리를 출력하며, 시간이 너무 부족하거나 넘칠 때의 안내 문구를 보여줌 | `result`, `max_time`, `preferred_part`, `preferred_level` | 없음 (화면 출력 수행) |

가장 중요한 함수 하나를 골라 자세히 설명하세요.

* 함수 이름: `generate_easy_routine`
* 설명:이 프로그램의 핵심 로직을 담당하는 함수입니다. 전체 데이터 중 사용자가 원하는 부위와 난이도가 맞는 운동을 1차로 필터링한 뒤, 내부 순서를 섞어 유연성을 줍니다. 이후 반복문을 순회하며 각 운동의 소요 시간을 누적해 나가다가, 사용자가 설정한 시간을 넘지 않는 안전한 범위 내에서만 운동을 최종 루틴 리스트에 추가하고 이를 반환합니다.

---

## 5. 조건문과 반복문 활용

### 조건문 사용 부분

어떤 조건을 검사했는가?

1. 사용자가 부위나 난이도를 입력할 때 정해진 단어 목록에 있는 게 맞는지 검사했습니다. (`if part in ["상체", "하체", "코어", "전신"]`)
2. 전체 데이터베이스를 검사하며 각 운동의 부위와 난이도가 사용자가 선호하는 조건과 둘 다 일치하는지 검사했습니다. (`if ex[3] == preferred_part and ex[4] == preferred_level`)
3. 장바구니에 운동을 담을 때, 현재까지 누적된 시간에 새로운 운동 시간을 더한 값이 사용자의 제한 시간 이하인지 검사했습니다. (`if current_time + ex[1] <= max_time`)
4. 입력한 시간이 너무 길어서 컴퓨터에 있는 모든 운동 시간을 다 더한 값보다 큰지 검사했습니다. (`if max_time > total_db_time`)

### 반복문 사용 부분

무엇을 반복 처리했는가?

1. 사용자가 오타를 내지 않고 완벽하게 올바른 단어나 숫자를 입력할 때까지 입력을 계속 다시 받는 과정을 반복했습니다. (`while True`)
2. 전체 운동 리스트 카드 더미를 맨 위에서부터 한 장씩 꺼내서 제한 시간 조건에 맞는지 검사하는 작업을 반복했습니다. (`for ex in total_pool`)
3. 최종 선택된 장바구니 속 운동들을 하나씩 꺼내어 영수증 모양으로 글자를 출력하고 총 칼로리를 누적 계산하는 작업을 반복했습니다. (`for ex in result`)

### 조건문과 반복문이 함께 작동한 부분

> `get_user_input` 함수 내부에서는 올바른 값이 들어올 때까지 `while`문으로 무한 반복하면서, `if`문으로 사용자가 정확한 메뉴 단어를 입력했는지 판단하여 통과시키거나 다시 쓰게 제어했습니다. 또한 `generate_easy_routine` 함수 내부에서는 전체 운동 목록을 `for`문으로 반복 순회하면서, 동시에 `if`문으로 시간이 넘지 않는지 조건을 검사하여 제한 시간 내에 딱 맞는 조합을 완성했습니다.

---

## 6. 실행 결과

```text
=== ✨ 나만의 맞춤 운동 루틴 매니저 ✨ ===
원하는 운동 부위를 입력하세요 (상체/하체/코어/전신): 하체
원하는 운동 난이도를 입력하세요 (초급/중급/상급): 중급
운동할 수 있는 최대 시간(분)을 입력하세요: 25

========================================
🏃‍♂️ [하체 / 중급] 중심의 25분 맞춤 루틴
========================================
- 런지 (하체/중급) : 8분 / 90kcal
- 스쿼트 (하체/초급) : 10분 / 120kcal
----------------------------------------
⏱ 실제 총 운동 시간: 18분
🔥 예상 총 소모 칼로리: 210kcal
========================================

```

결과 분석:

> 사용자가 '하체', '중급', '25분'을 입력했을 때, 난이도가 중급인 '런지'가 최우선으로 골라졌고, 시간이 남아서 예비 목록에 있던 초급 하체 운동인 '스쿼트'까지 안전하게 담겼습니다. 총 걸린 시간은 18분으로 제한 시간 25분을 정확하게 지켰으며, 칼로리 합계도 210kcal로 알맞게 출력된 것을 확인할 수 있습니다.

---

## 7. 테스트와 오류 수정

### 테스트 1: 정상 사례

* 입력: 부위 = `"하체"`, 난이도 = `"중급"`, 제한 시간 = `25`
* 예상 결과: 중급 하체 운동 위주로 먼저 구성되며, 총 시간이 25분 이하인 루틴 출력
* 실제 결과: 런지(8분)와 스쿼트(10분)가 선택되어 총 18분짜리 루틴 정상 출력

### 테스트 2: 예외 또는 경계 사례

* 입력: 부위 = `"하채"` (오타 입력)
* 예상 결과: 잘못된 단어이므로 통과되지 않고 경고 메시지가 나온 뒤 다시 입력하라고 나와야 함
* 실제 결과: "❌ 잘못된 부위입니다. 다시 입력해 주세요!"가 출력되며 프로그램이 멈추지 않고 재입력창이 뜨는 것 확인

### 발견한 오류와 수정한 방법

> 처음에 사용자가 최대 시간에 숫자가 아닌 "삼십분" 같은 글자를 치면 프로그램이 에러를 내며 빨간 줄과 함께 툭 꺼져버리는 오류가 있었습니다. 이를 해결하기 위해 `try-except` 문법을 사용해 에러를 안전하게 가로챘고, 오류가 나는 대신 "숫자로만 정확하게 입력해 주세요!"라는 경고문이 나오면서 다시 기분 좋게 입력창으로 돌아가도록 수정했습니다.

---

## 8. Gemini 활용 내용 요약

Gemini에게 도움받은 부분:

> 2차원 리스트 데이터를 인덱스 번호(`ex[0]`, `ex[1]`)로 헷갈리지 않고 꺼내는 기본 구조를 잡는 데 도움을 받았습니다. 특히 사용자가 오타를 내거나 글자를 잘못 입력했을 때 프로그램이 멈추지 않고 재입력을 받도록 방어하는 알고리즘 아이디어를 얻었습니다.

Gemini가 제안했지만 내가 수정하거나 사용하지 않은 부분:

> Gemini는 운동의 시간 대비 칼로리 효율성을 수학적으로 계산하여 줄 세우는 어려운 정렬 알고리즘을 제안했으나, 우리 학교 수행평가의 목적과 제가 직접 설명할 수 있는 코드 난이도를 고려해 복잡한 정렬 알고리즘은 제외하고 '순수한 조건 필터링과 무작위 섞기' 기능에만 집중하여 사용하지 않았습니다.

최종 코드에서 내가 직접 판단하거나 수정한 부분:

> 초기 코드에는 난이도 구분이 없어서 고를 수 있는 옵션이 너무 적다는 아쉬움이 있었습니다. 그래서 제가 직접 데이터베이스와 입력창 목록에 '중급'이라는 선택지를 새로 추가하여 프로그램의 활용도를 높였고, 사용자가 시간을 너무 크게 입력했을 때를 대비한 '전체 데이터 총합 비교 안내 문구'를 출력창에 직접 추가해 수정했습니다.

---

## 9. 한계와 개선 방안

현재 프로그램의 한계:

> 부위나 난이도에 사용자가 '하체', '초급' 같은 정해진 단어만 쳐야 정상 작동하고, '하체운동할래요'나 '초급자용'처럼 조금만 다르게 문장으로 입력하면 오타로 인식해 재입력을 요구하는 융통성 부족의 한계가 있습니다.

다시 개선한다면 추가하고 싶은 기능:

> 파이썬의 문자열 포함 문법(`in`)을 더 적극적으로 활용하여, 사용자가 '하체운동'이라고 길게 입력해도 단어 안에 '하체'라는 단어가 들어있다면 자동으로 인식해 주는 '유연한 키워드 검색 시스템'을 추가하고 싶습니다.

---

## 10. 코드인터뷰 준비

1. 내 프로그램의 입력-처리-출력은 무엇인가?

> 사용자가 원하는 부위, 난이도, 최대 가용 시간을 입력하면, 프로그램이 오타를 검사하고 2차원 리스트에서 조건에 맞는 운동을 필터링하여 제한 시간 내로 조합하는 처리를 한 뒤, 최종 루틴 영수증과 실제 시간, 총 칼로리를 화면에 출력합니다.

2. 2차원 리스트에서 한 행과 각 열은 무엇을 의미하는가?

> 한 행은 '하나의 운동 정보 세트'를 의미합니다. 0번 열은 이름, 1번 열은 걸리는 시간, 2번 열은 타는 칼로리, 3번 열은 운동 부위, 4번 열은 운동 난이도를 뜻합니다.

3. 가장 중요한 함수는 무엇이고 어떤 역할을 하는가?

> `generate_easy_routine` 함수입니다. 입력된 조건을 바탕으로 부위와 난이도 필터링을 하고, 무작위로 순서를 섞은 뒤, 제한 시간을 넘지 않는 선에서 운동을 쏙쏙 골라 담아 루틴 조합을 완성하는 핵심 역할을 합니다.

4. 조건문과 반복문은 어디에서 사용되었는가?

> `while True` 반복문은 사용자가 정확한 값을 입력할 때까지 감시할 때 썼고, `for` 반복문은 운동 리스트를 순회하며 장바구니에 담거나 화면에 영수증을 출력할 때 썼습니다. `if` 조건문은 오타가 났는지 확인할 때, 조건에 맞는 운동인지 볼 때, 그리고 누적 시간이 제한 시간을 넘지 않는지 판단할 때 요긴하게 사용되었습니다.

5. Gemini가 도와준 부분과 내가 직접 수정한 부분은 무엇인가?

> Gemini는 전체적인 2차원 리스트 매핑 구조와 `try-except` 에러 방어 코드를 짜는 것을 도와주었습니다. 저는 프로그램의 완성도를 높이기 위해 '중급' 난이도 데이터를 직접 기획해서 추가했고, 사용자가 너무 큰 시간을 입력했을 때 안내를 띄워주는 예외 로직을 직접 판단하여 코드를 업그레이드했습니다.