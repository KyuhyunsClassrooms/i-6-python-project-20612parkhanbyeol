이번에는 학교에 제출할 **[최종 보고서]** 양식이군요! 앞서 기획했던 내용과 완성된 파이썬 코드를 바탕으로, 선생님께서 채점하시기 좋게 학술적이고 깔끔한 문체로 빈칸을 완벽하게 채워 드립니다.

그대로 복사해서 보고서를 완성해 보세요. 화이팅입니다! 👍

---

# 최종 보고서

## 1. 프로그램 주제와 목적

**내 프로그램의 주제:**

> 사용자의 선호 부위와 제한 시간에 맞춘 '조건 필터링형' 운동 루틴 추천기

**이 프로그램을 만든 이유:**

> 바쁜 일상 속에서 운동을 계획할 때 "오늘 무슨 운동을 조합해야 하지?"라는 선택의 피로감을 줄이기 위함입니다. 사용자가 원하는 운동 부위와 가용 시간을 입력하면, 조건에 맞는 최적의 운동 루틴을 자동으로 구성해 줌으로써 시간 낭비를 막고 효율적인 운동 체계를 제공하고자 만들었습니다.

---

## 2. 문제 분해 및 모델링

### 입력

* 사용자가 입력하는 값: 선호 운동 부위 (문자열: 상체, 하체, 코어, 전신 등), 가용 제한 시간 (정수: 분 단위)
* 데이터베이스 데이터: 운동 종목별 정보 (이름, 소요 시간, 소소 칼로리, 부위)

### 처리

1. **데이터 필터링:** 2차원 리스트 내부를 순회하며 사용자가 입력한 선호 부위와 일치하는 데이터만 1차로 추출합니다.
2. **데이터 유연화(셔플):** 동일한 조건에서도 매번 새로운 루틴을 제공할 수 있도록 데이터의 순서를 무작위로 섞어줍니다.
3. **조건부 누적 제어:** 반복문을 통해 운동을 하나씩 루틴 리스트에 추가하며, 이때 누적 운동 시간이 사용자가 지정한 제한 시간을 초과하지 않도록 검사하고 차곡차곡 담습니다.

### 출력

* 사용자의 조건에 부합하는 **최종 운동 루틴 리스트 (운동명, 부위, 시간, 칼로리)**
* 루틴에 포함된 운동들의 **'실제 총 소요 시간'** 및 **'예상 총 소모 칼로리'**

---

## 3. 사용한 2차원 리스트 설명

**2차원 리스트 이름:** `exercise_db`

**한 행의 의미:**

> 하나의 독립된 운동 종목이 가지는 속성 정보의 집합

**각 열의 의미:**

| 열 번호 | 의미 | 실제 예시 |
| --- | --- | --- |
| 0번 열 | 운동 이름 (문자열) | "스쿼트", "푸쉬업" |
| 1번 열 | 운동 소요 시간 (정수, 분 단위) | 10, 5 |
| 2번 열 | 예상 소모 칼로리 (정수, kcal 단위) | 120, 50 |
| 3번 열 | 운동 타겟 부위 (문자열) | "하체", "상체" |

**이 2차원 리스트를 사용한 이유:**

> 하나의 운동을 표현하기 위해서는 이름, 시간, 칼로리, 부위라는 서로 다른 형태의 데이터(문자열, 정수)들이 유기적으로 연결되어 있어야 합니다. 2차원 리스트를 사용하면 인덱스 번호(0~3)를 활용해 데이터의 연관성을 유지하면서 대량의 운동 목록을 행 단위로 깔끔하고 체계적으로 관리할 수 있기 때문입니다.

---

## 4. 함수 설명

| 함수 이름 | 역할 | 입력값 | 반환값 또는 출력 |
| --- | --- | --- | --- |
| `get_user_input` | 사용자로부터 원하는 부위와 제한 시간을 오류 없이 입력받음 | 없음 | `preferred_part` (문자열), `max_time` (정수) |
| `generate_routine` | 조건에 맞는 운동을 필터링하고 제한 시간 내로 루틴을 조합함 | `max_time`, `preferred_part` | `selected_routine` (선택된 운동들의 2차원 리스트) |
| `print_result` | 최종 추천 루틴과 총 시간, 총 칼로리를 가독성 있게 출력함 | `selected_routine` | 없음 (화면 출력 수행) |

**가장 중요한 함수 하나를 골라 자세히 설명하세요.**

* **함수 이름:** `generate_routine`
* **설명:** 이 프로그램의 핵심 로직을 담당하는 함수입니다. 리스트 컴프리헨션을 이용해 전체 데이터 중 사용자가 원하는 부위의 운동을 1차로 필터링한 뒤, 내부 순서를 섞어 유연성을 줍니다. 이후 반복문을 순회하며 각 운동의 소요 시간을 누적해 나가다가, 사용자가 설정한 `max_time`을 넘지 않는 안전한 범위 내에서만 운동을 최종 루틴 리스트에 추가하고 이를 반환합니다.

---

## 5. 조건문과 반복문 활용

### 조건문 사용 부분

**어떤 조건을 검사했는가?**

1. 전체 데이터베이스를 검사하며 각 운동의 부위가 사용자가 선호하는 부위와 일치하는지 검사했습니다. (`ex[3] == preferred_part`)
2. 루틴에 운동을 담을 때, 현재까지 누적된 운동 시간에 새로운 운동 시간을 더한 값이 사용자의 제한 시간 이하인지 검사했습니다. (`current_time + ex[1] <= max_time`)

### 반복문 사용 부분

**무엇을 반복 처리했는가?**

1. `exercise_db` 2차원 리스트의 첫 번째 행부터 마지막 행까지 하나씩 순회하며 필터링 조건을 검사하는 작업을 반복했습니다.
2. 최종 선택된 루틴 리스트 안의 운동들을 하나씩 꺼내어 화면에 포맷팅된 문자열로 출력하고 총합 점수를 누적 계산하는 작업을 반복했습니다.

### 조건문과 반복문이 함께 작동한 부분

> `generate_routine` 함수 내부에서 후보 운동 목록 전체를 `for`문으로 **반복 순회**하면서, 동시에 `if`문을 사용해 현재 누적 시간에 해당 운동 시간을 더해도 제한 시간을 초과하지 않는지 **조건을 검사**했습니다. 이 제어 구조를 통해 제한 시간을 칼같이 지키면서 최대로 담을 수 있는 맞춤형 조합을 완성할 수 있었습니다.

---

## 6. 실행 결과

**대표 실행 결과를 붙여넣으세요.**

```text
🏃‍♂️ [하체] 중심의 25분 맞춤 루틴 🏃‍♂️

- 스쿼트 (하체) : 10분 / 120kcal
- 런지 (하체) : 8분 / 90kcal

⏱ 실제 총 운동 시간: 18분
🔥 예상 총 소모 칼로리: 210kcal

```

**결과 분석:**

> 사용자가 '하체', '25분'을 입력했을 때, 프로그램이 정확하게 하체 운동인 '스쿼트'와 '런지'를 골라냈습니다. 두 운동의 시간 합은 18분으로 제한 시간인 25분을 넘지 않으면서 안전하게 구성되었으며, 칼로리 합산 수치(210kcal)도 정확하게 계산되어 출력되었음을 확인할 수 있습니다.

---

## 7. 테스트와 오류 수정

### 테스트 1: 정상 사례

* **입력:** 선호 부위 = `"하체"`, 제한 시간 = `25`
* **예상 결과:** 하체 운동 위주로 구성되며, 총 시간이 25분 이하인 루틴 출력
* **실제 결과:** 스쿼트(10분)와 런지(8분)가 선택되어 총 18분짜리 루틴 정상 출력

### 테스트 2: 예외 또는 경계 사례

* **입력:** 선호 부위 = `"하체"`, 제한 시간 = `3`
* **예상 결과:** 가장 짧은 하체 운동(런지 8분)보다 소용 시간이 작으므로, 아무 운동도 담기지 않거나 경고 메시지 출력
* **실제 결과:** 운동 누적 시간이 제한 시간인 3을 바로 초과하여 빈 루틴이 출력되거나 에러가 날 수 있는 불안정한 상태 확인

### 발견한 오류와 수정한 방법

> 2차원 리스트에서 데이터를 꺼낼 때 처음에 `ex["time"]` 형태로 접근했다가 `TypeError` 에러가 발생했습니다. 확인해 보니 딕셔너리가 아닌 2차원 리스트 구조였기 때문에, 인덱스 번호를 사용해야 함을 깨닫고 `ex[1]`(시간), `ex[2]`(칼로리) 형태로 명시적인 숫자를 넣어 인덱싱 접근 오류를 올바르게 수정했습니다.

---

## 8. Gemini 활용 내용 요약

**Gemini에게 도움받은 부분:**

> 복잡하고 동적인 알고리즘(DP) 대신 '쇼핑몰 필터링 조건'이라는 직관적인 구조로 아이디어를 기획하고 문제를 세분화하는 과정에서 큰 도움을 받았습니다. 보고서의 서식을 논리적인 문장으로 정립하는 데 기여했습니다.

**Gemini가 제안했지만 내가 수정하거나 사용하지 않은 부분:**

> Gemini는 가성비(시간 대비 칼로리 효율성) 정렬 알고리즘 코드를 추가하는 것을 제안했으나, 프로그래밍 수행평가의 기본 목적과 가독성을 고려해 복잡한 정렬 수식은 제외하고 순수한 '필터링과 제한 시간 조건 조건문'에만 집중하도록 코드를 정제하여 사용하지 않았습니다.

**최종 코드에서 내가 직접 판단하거나 수정한 부분:**

> 데이터베이스의 구조를 직관적인 딕셔너리 리스트에서 학교 과제 조건인 **'2차원 리스트(List of Lists)'** 구조로 변경함에 따라, 코드 내의 모든 데이터 접근 방식을 인덱스 구조(`ex[0]`, `ex[1]`)로 직접 치환하고 매핑 관계를 확인하며 수정했습니다.

---

## 9. 한계와 개선 방안

**현재 프로그램의 한계:**

> 사용자가 제한 시간을 너무 짧게 설정해 필터링된 운동을 단 하나도 담지 못하는 경계 조건(예: 3분 입력)이 되었을 때, 예외 처리나 "시간을 늘려주세요" 같은 친절한 안내 문구가 부족하여 빈 결과만 나오는 아쉬움이 있습니다.

**다시 개선한다면 추가하고 싶은 기능:**

> 파이썬의 `try-except` 구조를 명확히 도입하여 사용자가 잘못된 문자열을 입력하거나 비정상적인 범위를 입력했을 때 자동으로 재입력을 유도하는 **'입력값 검증 시스템'**을 추가하고 싶습니다.

---

## 10. 코드인터뷰 준비

**1. 내 프로그램의 입력-처리-출력은 무엇인가?**

> 사용자가 원하는 운동 부위와 가용 시간을 **입력**하면, 프로그램 내부 2차원 리스트에서 일치하는 데이터를 골라내어 제한 시간 내로 담는 **처리**를 거친 후, 최종 완성된 맞춤 운동 목록과 총 시간, 총 칼로리를 **출력**합니다.

**2. 2차원 리스트에서 한 행과 각 열은 무엇을 의미하는가?**

> 한 행은 '하나의 운동 종목 세트'를 의미하며, 0번 열은 운동 이름, 1번 열은 소요 시간, 2번 열은 소모 칼로리, 3번 열은 타겟 부위를 의미합니다.

**3. 가장 중요한 함수는 무엇이고 어떤 역할을 하는가?**

> `generate_routine` 함수입니다. 입력된 조건을 바탕으로 부위 필터링, 무작위 순서 섞기, 제한 시간 만족 여부 검사를 모두 수행하여 최종 결과 리스트를 조합해 내는 핵심 두뇌 역할을 합니다.

**4. 조건문과 반복문은 어디에서 사용되었는가?**

> 반복문은 전체 운동 리스트를 순회하거나 최종 루틴을 출력할 때 사용되었고, 조건문은 사용자가 원하는 부위와 일치하는지 확인하거나 누적 운동 시간이 제한 시간을 넘지 않는지 검사할 때 사용되었습니다.

**5. Gemini가 도와준 부분과 내가 직접 수정한 부분은 무엇인가?**

> Gemini는 쇼핑몰 필터링 방식의 아이디어 구체화와 보고서 문장 작성을 도와주었고, 저는 과제 조건에 맞춰 딕셔너리가 아닌 **2차원 리스트 인덱스 구조**로 코드를 수정하고 불필요한 고난도 정렬 알고리즘을 덜어내어 완성도를 높였습니다.