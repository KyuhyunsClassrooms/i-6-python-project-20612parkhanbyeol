
## 1. 프로젝트 주제

내가 만들 프로그램의 주제:

> 사용자의 선호 부위와 제한 시간에 맞춘 '조건 필터링형' 운동 루틴 추천기

---

## 2. 문제 상황

이 프로그램이 해결하려는 문제를 설명하세요.

내 설명:

> 사용자가 운동하고 싶은 부위와 사용할 수 있는 시간을 입력하면, 수많은 운동 종류 중 무엇을 해야 할지 고민하는 선택의 피로감을 해결하고, 제한 시간 내에 딱 맞는 최적의 맞춤 운동 조합을 추천해 주고 싶다.
---

### 입력

사용자가 입력하거나 프로그램이 사용할 데이터는 무엇인가?

> * 선호 부위(문자열), 선호 난이도(문자열), 운동 제한 시간(정수)
> * 운동 목록 데이터베이스(운동 이름, 소요 시간, 소모 칼로리, 타겟 부위,난이도)
> 
> 

### 처리

입력된 데이터를 어떤 규칙이나 알고리즘으로 처리할 것인가?

> * 전체 운동 목록 중 사용자가 입력한 부위와 난이도가 동시에 일치하는 데이터만 골라낸다.
> *  사용자가 지정된 부위나 난이도 외의 단어를 입력하면 반복문(`while`)으로 재입력을 요구하고, 시간에 숫자가 아닌 문자를 입력하면 예외 처리(`try-except`)로 걸러낸다.
> * 지루하지 않도록 조건에 맞는 운동들의 순서를 유연하게 섞는다.
> *  반복문을 돌며 제한 시간을 넘지 않을 때까지만 장바구니에 차곡차곡 더한다.
> 
> 

---

## 4. 2차원 리스트 설계 (수정)

내 프로그램에서 사용할 2차원 리스트의 이름: `exercise_db`

```python
exercise_db = [
    ["스쿼트", 10, 120, "하체", "초급"],
    ["런지", 8, 90, "하체", "중급"],
    ["레그프레스", 15, 140, "하체", "상급"],
    ["푸쉬업", 5, 50, "상체", "초급"],
    ["턱걸이", 7, 80, "상체", "상급"],
    ["바벨로우", 12, 100, "상체", "중급"],
    ["플랭크", 4, 30, "코어", "초급"],
    ["크런치", 6, 45, "코어", "중급"],
    ["버피테스트", 12, 180, "전신", "상급"]
]

```

### 각 열의 의미 (4번 열 난이도 추가)

| 열 번호 | 의미 | 예시 |
| --- | --- | --- |
| 0번 열 | 운동 이름 (문자열) | "스쿼트" |
| 1번 열 | 운동 소요 시간 (정수, 분 단위) | 10 |
| 2번 열 | 예상 소모 칼로리 (정수, kcal 단위) | 120 |
| 3번 열 | 운동 타겟 부위 (문자열) | "하체" |
| 4번 열 | 운동 난이도 (문자열) | "초급" / "중급" / "상급" |

---

## 5. 함수 설계 (수정)

| 함수 이름 | 역할 | 입력값 | 반환값 또는 출력 |
| --- | --- | --- | --- |
| `get_user_input` | 원하는 부위, 난이도, 시간을 오타/형변환 예외 처리 검증을 거쳐 입력받음 | 없음 | `part`, `level`, `time` |
| `generate_easy_routine` | 조건에 맞는 운동을 필터링하고 제한 시간 내로 조합함 | `max_time`, `preferred_part`, `preferred_level` | `selected_routine` (선택된 운동들의 2차원 리스트) |
| `print_result` | 최종 추천 루틴을 출력하며, 시간 초과 및 데이터 부족 예외 상황 안내 문구를 출력함 | `result`, `max_time`, `preferred_part`, `preferred_level` | 없음 (화면 출력) |

---

## 6. 사용할 제어구조 (추가 및 수정)

### 조건문(`if`)을 사용할 부분

> 1. 운동 데이터 중 부위와 난이도가 사용자가 입력한 조건과 일치하는지 확인할 때 사용한다. (`if ex[3] == preferred_part and ex[4] == preferred_level`)
> 2. 누적 시간 조건 검사 및 사용자가 올바른 메뉴 단어를 입력했는지 판정할 때 사용한다. (`if part in [...]`)
> 3. 제한 시간이 너무 짧아 추천 결과 리스트가 비어있는지 판정할 때 사용한다. (`if not result`)
> 4. 사용자가 입력한 시간이 DB에 있는 모든 운동 시간의 합보다 큰지 판정할 때 사용한다. (`if max_time > total_db_time`)
> 
> 

### 반복문(`while` / `for`)을 사용할 부분

> 1. `while True`: 사용자가 오타를 내거나 잘못된 값을 입력했을 때, 정확한 값을 입력할 때까지 무한히 재입력을 요청하기 위해 사용한다.
> 2. `for ex in total_pool`: 2차원 리스트의 운동 데이터를 하나씩 꺼내어 제한 시간 누적 계산을 할 때 사용한다.
> 3. `for ex in result`: 최종 선택된 장바구니 리스트를 순회하며 화면에 출력하고 총 칼로리를 계산할 때 사용한다.
> 
> 

---

## 8. Gemini에게 도움받고 싶은 부분 (내용 정제)

> 1. 딕셔너리가 아닌 2차원 리스트를 사용할 때 인덱스 번호(`ex[0]`, `ex[1]`)를 활용해 직관적으로 데이터를 매핑하는 방법
> 2. 사용자가 문자를 입력해 `ValueError`가 발생할 때 프로그램이 터지지 않도록 보호하는 `try-except` 예외 처리 구조 구현 방법
> 3. 사용자가 입력 양식에 맞지 않는 오타를 냈을 때 무한 루프로 재입력을 유도하는 데이터검증 알고리즘 구현 방법