
## 1. 프로젝트 주제

내가 만들 프로그램의 주제:

> 사용자의 선호 부위와 제한 시간에 맞춘 '조건 필터링형' 운동 루틴 추천기

---

## 2. 문제 상황

이 프로그램이 해결하려는 문제를 설명하세요.

내 설명:

> 사용자가 운동하고 싶은 부위와 사용할 수 있는 시간을 입력하면, 수많은 운동 종류 중 무엇을 해야 할지 고민하는 선택의 피로감을 해결하고, 제한 시간 내에 딱 맞는 최적의 맞춤 운동 조합을 추천해 주고 싶다.

---

## 3. 입력-처리-출력 구조

### 입력

사용자가 입력하거나 프로그램이 사용할 데이터는 무엇인가?

> * 사용자 입력 데이터: 선호 부위(문자열), 운동 제한 시간(정수)
> * 프로그램 내부 데이터: 운동 목록 데이터베이스(운동 이름, 소요 시간, 소모 칼로리, 타겟 부위)
> 
> 

### 처리

입력된 데이터를 어떤 규칙이나 알고리즘으로 처리할 것인가?

> * 필터링: 전체 운동 목록 중 사용자가 입력한 부위와 일치하는 데이터만 쏙 골라낸다.
> * 무작위 섞기(셔플): 매번 똑같은 루틴이 나와서 지루해지지 않도록 조건에 맞는 운동들의 순서를 유연하게 섞는다.
> * 조건부 누적:반복문을 돌며 운동을 하나씩 담다가, 누적 시간이 사용자가 입력한 제한 시간을 넘지 않을 때까지만 차곡차곡 더한다.
> 
> 

### 출력

프로그램이 최종적으로 무엇을 출력하는가?

> * 조건에 맞춰 최종 구성된 '오늘의 맞춤 운동 루틴 목록'
> * 루틴 내 운동들의 '실제 총 운동 시간' 및 '예상 총 소모 칼로리'
> 
> 

---

## 4. 2차원 리스트 설계

내 프로그램에서 사용할 2차원 리스트의 이름: `exercise_db`

```python
# 예시 형식
data = [
    ["스쿼트", 10, 120, "하체"],
    ["런지", 8, 90, "하체"],
    ["푸쉬업", 5, 50, "상체"],
    ["플랭크", 4, 30, "코어"],
    ["버피테스트", 12, 180, "전신"],
]

```

### 한 행의 의미

> 하나의 독립된 운동 종목에 대한 세부 정보 세트

### 각 열의 의미

| 열 번호 | 의미 | 예시 |
| --- | --- | --- |
| 0번 열 | 운동 이름 (문자열) | "스쿼트" |
| 1번 열 | 운동 소요 시간 (정수, 분 단위) | 10 |
| 2번 열 | 예상 소모 칼로리 (정수, kcal 단위) | 120 |
| 3번 열 | 운동 타겟 부위 (문자열) | "하체" |

---

## 5. 함수 설계

| 함수 이름 | 역할 | 입력값 | 반환값 또는 출력 |
| --- | --- | --- | --- |
| `get_user_input` | 사용자로부터 원하는 부위와 제한 시간을 입력받음 | 없음 | `preferred_part`, `max_time` |
| `generate_routine` | 조건에 맞는 운동을 필터링하고 제한 시간 내로 조합함 | `max_time`, `preferred_part` | `selected_routine` (선택된 운동들의 2차원 리스트) |
| `print_result` | 최종 추천 루틴과 총 시간, 총 칼로리를 보기 좋게 출력함 | `selected_routine` | 없음 (화면 출력) |

---

## 6. 사용할 제어구조

### 조건문을 사용할 부분

내 계획:

> 1. 운동 데이터 중 3번 열(부위)이 사용자가 입력한 선호 부위와 일치하는지 확인할 때 사용한다. (`if ex[3] == preferred_part`)
> 2. 현재까지 누적된 운동 시간에 새로운 운동 시간을 더한 값이 사용자의 제한 시간 이하인지 확인할 때 사용한다. (`if current_time + ex[1] <= max_time`)
> 
> 

### 반복문을 사용할 부분

예) 2차원 리스트의 모든 항목을 하나씩 검사한다.

내 계획:

> 1. 2차원 리스트 `exercise_db`의 모든 행(운동 종목)을 하나씩 검사하며 필터링할 때 사용한다.
> 2. 최종 선택된 루틴 리스트를 하나씩 순회하며 화면에 운동 이름과 정보를 출력하고 총합을 계산할 때 사용한다.
> 
> 

---

## 7. 예상되는 예외 상황

1. 사용자가 제한 시간에 숫자가 아닌 문자(예: "삼십분")를 입력하여 데이터 형변환 오류가 발생하는 상황
2. 사용자가 제한 시간을 너무 짧게 입력하여(예: 2분) 데이터베이스에 있는 어떤 운동도 기준 시간을 초과해 담지 못하는 상황

---

## 8. Gemini에게 도움받고 싶은 부분


1. 딕셔너리가 아닌 2차원 리스트를 사용할 때 인덱스 번호(`ex[0]`, `ex[1]`)를 헷갈리지 않고 가독성 좋게 코드를 작성하는 방법
2. 사용자가 숫자가 아닌 값을 입력했을 때 프로그램이 멈추지 않도록 하는 예외 처리 방법
3. 조건에 맞는 운동들을 단순히 무작위가 아니라 '시간 대비 칼로리 효율'이 높은 순서로 먼저 고려되도록 정렬하는 방법